# Production URL for SEO (sitemap, canonical, Open Graph). Default: https://flowqualify.app
# NEXT_PUBLIC_SITE_URL=https://flowqualify.app

# Google Search Console: add after verifying your property to output <meta name="google-site-verification" content="...">
# GOOGLE_SITE_VERIFICATION=your-verification-string

# Get your API key at https://resend.com (free tier: 100 emails/day)
RESEND_API_KEY=re_xxxxxxxxxxxx

# Optional. "From" address shown when you receive the contact email (e.g. "FlowQualify Contact <...>").
# - Omit this to use onboarding@resend.dev (fine for testing).
# - For production: verify your domain in Resend, then set e.g. contact@yourdomain.com
# The person filling the form already enters their email in the form; that becomes Reply-To.
# RESEND_FROM_EMAIL=contact@yourdomain.com

# --- Vercel Blob (for video hosting) ---
# 1. Create a Blob store: Vercel Dashboard → Storage → Create Database → Blob
# 2. Copy BLOB_READ_WRITE_TOKEN into .env (do not commit .env)
# 3. Run: npm run upload-videos
# 4. Paste the printed NEXT_PUBLIC_VIDEO_* lines into .env and into Vercel Environment Variables
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxx

# Video URLs: set these after running npm run upload-videos (or after uploading to your own CDN)
# NEXT_PUBLIC_VIDEO_FLOW_DEMO=
# NEXT_PUBLIC_VIDEO_DEMO_1=
# NEXT_PUBLIC_VIDEO_DEMO_2=
